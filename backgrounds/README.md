# Custom backgrounds

Drop your own images here named `ghibli-01.jpg` through `ghibli-20.jpg`
(also supports .jpeg, .png, .webp). The app picks them up automatically on
next launch and uses them instead of the built-in Unsplash landscapes.
